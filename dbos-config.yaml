# DBOS Configuration for dbos-go-starter
name: dbos-go-starter
language: go
runtimeConfig:
  start:
    - go run main.go
database_url: ${DBOS_SYSTEM_DATABASE_URL}
database:
  migrate:
    - echo "your migration commands here"